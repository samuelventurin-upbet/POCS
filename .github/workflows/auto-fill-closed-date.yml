name: Auto-fill Closed Date

permissions:
  issues: write
  contents: write

on:
  issues:
    types: [closed]

env:
  GITHUB_TOKEN: ${{ secrets.PAT }}

jobs:
  update-closed-date:
    runs-on: ubuntu-latest
    steps:
      - name: Set Closed Date
        uses: actions/github-script@v4
        with:
          script: |
            const issue_number = context.payload.issue.number;
            const closed_date = new Date().toISOString().split('T')[0];
            const existing_labels = context.payload.issue.labels.map(label => label.name);
            
            // Remove any existing 'closed-date' labels
            const updated_labels = existing_labels.filter(label => !label.startsWith('closed-date'));

            // Add the new 'closed-date' label
            updated_labels.push(`closed-date:${closed_date}`);

            await github.issues.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issue_number,
              labels: updated_labels
            });